class classes(object):
    class a(object):
        def __init__(self, name, assignment, due):
            self.name = name
            self.assignment = assignment
            self.due = due
    class b(object):
        def __init__(self, name, assignment, due):
            self.name = name
            self.assignment = assignment
            self.due = due
    class c(object):
        def __init__(self, name, assignment, due):
            self.name = name
            self.assignment = assignment
            self.due = due
    class d(object):
        def __init__(self, name, assignment, due):
            self.name = name
            self.assignment = assignment
            self.due = due
    class e(object):
        def __init__(self, name, assignment, due):
            self.name = name
            self.assignment = assignment
            self.due = due
    class f(object):
        def __init__(self, name, assignment, due):
            self.name = name
            self.assignment = assignment
            self.due = due
    class g(object):
        def __init__(self, name, assignment, due):
            self.name = name
            self.assignment = assignment
            self.due = due